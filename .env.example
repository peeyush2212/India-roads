# Optional: Stripe checkout (demo)
# If not set, the Stripe checkout button shows "not configured".

STRIPE_SECRET_KEY=
# Optional: use a pre-created Price ID instead of price_data
STRIPE_PRICE_ID=

# Optional: for building absolute URLs when origin header isn't present
NEXT_PUBLIC_SITE_URL=http://localhost:3000
